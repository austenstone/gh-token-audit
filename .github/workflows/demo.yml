name: Demo audit

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "dist/**"
      - "action.yml"
      - ".github/workflows/demo.yml"

permissions:
  contents: read

jobs:
  demo:
    name: Audit a sample repo
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Run gh-token-audit
        uses: ./
        with:
          repo: austenstone/actions-playground
          fail-on: none
